[package]
name = "metacleaner-text"
description = "Strip invisible-Unicode steganography (zero-width chars, bidi overrides, Unicode Tag block, variation-selector smuggling) from plain text. Pure string processing, no dependencies — same portability goal as metacleaner-core."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
